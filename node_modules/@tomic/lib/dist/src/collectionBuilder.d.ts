import { Collection } from './collection.js';
import { Store } from './store.js';

export declare class CollectionBuilder {
    private store;
    private server;
    private params;
    constructor(store: Store, server?: string);
    setProperty(property: string): CollectionBuilder;
    setValue(value: string): CollectionBuilder;
    setSortBy(sortBy: string): CollectionBuilder;
    setSortDesc(sortDesc: boolean): CollectionBuilder;
    setPageSize(pageSize: number): CollectionBuilder;
    build(): Collection;
    buildAndFetch(): Promise<Collection>;
}
//# sourceMappingURL=collectionBuilder.d.ts.map