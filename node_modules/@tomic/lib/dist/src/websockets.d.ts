import { Resource } from './resource.js';
import { Store } from './store.js';

/** Opens a Websocket Connection at `/ws` for the current Drive */
export declare function startWebsocket(url: string, store: Store): WebSocket;
/**
 * Authenticates current Agent over current WebSocket. Doesn't do anything if
 * there is no agent
 */
export declare function authenticate(client: WebSocket, store: Store, fetchAll?: boolean): Promise<void>;
/** Sends a GET message for some resource over websockets. */
export declare function fetchWebSocket(client: WebSocket, subject: string): Promise<Resource>;
//# sourceMappingURL=websockets.d.ts.map