import { Resource } from './resource.js';
import { JSONObject, JSONValue } from './value.js';

export declare class JSONADParser {
    private parsedResources;
    /**
     * Parses an JSON-AD object containing a resource. Returns the resource and a list of all the sub-resources it found.
     */
    parseObject(jsonObject: JSONObject, resourceSubject?: string): [parsedRootResource: Resource, allParsedResources: Resource[]];
    /**
     * Parses an array of JSON-AD objects containing resources.
     * Returns a list of the resources in the array and a list of all the resources that were found including sub-resources.
     */
    parseArray(jsonArray: unknown[]): [resourcesInArray: Resource[], allParsedResources: Resource[]];
    parseValue(value: JSONValue, key: string): [value: JSONValue, allParsedResources: Resource[]];
    private parseJsonADResource;
    private parseJsonAdResourceValue;
    /** Parses a JSON-AD array, returns array of Resources */
    private parseJsonADArray;
}
//# sourceMappingURL=parse.d.ts.map